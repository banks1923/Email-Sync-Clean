# Load secrets from centralized location
source ~/Secrets/.env

# Project-specific settings
export PYTHONPATH=$PWD
export LOG_LEVEL=INFO
export QDRANT__STORAGE__PATH=./qdrant_data
export QDRANT__LOG__PATH=./logs/qdrant.log
export MCP_STORAGE_DIR=$PWD/data/sequential_thinking

# Optional: Show loaded status
echo "📧 Email-Sync environment loaded from ~/Secrets/.env"
echo "   PYTHONPATH: $PYTHONPATH"
echo "   API Keys: $([ -n "$ANTHROPIC_API_KEY" ] && echo "✓ Loaded" || echo "✗ Missing")"