# Requirements for Standalone Legal Document Boilerplate Removal Tool

# Core OCR dependencies
pytesseract>=0.3.10          # Python wrapper for Tesseract OCR
pdf2image>=3.1.0             # Convert PDF pages to images for OCR
Pillow>=10.0.0               # Image processing for OCR

# NLP and ML dependencies (optional but recommended)
scikit-learn>=1.3.0          # For similarity detection and TF-IDF
numpy>=1.24.0                # Required by scikit-learn
spacy>=3.7.0                 # For advanced text processing (optional)

# Utility dependencies
loguru>=0.7.0               # Better logging (used by existing components)

# Note: You also need to install Tesseract OCR system dependency
# 
# Ubuntu/Debian:
#   sudo apt-get install tesseract-ocr tesseract-ocr-eng poppler-utils
# 
# macOS:
#   brew install tesseract poppler
# 
# Windows:
#   Download from: https://github.com/UB-Mannheim/tesseract/wiki
#   Download poppler from: https://github.com/oschwartz10612/poppler-windows
#
# spaCy model (optional):
#   python -m spacy download en_core_web_sm
