google-api-python-client==2.116.0
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.2.0
requests
openai
loguru>=0.7.0            # Superior logging with automatic rotation and structured output
qdrant-client
pypdf>=5.0.0  # Modern PDF processing (successor to PyPDF2)
pdfplumber
# OCR dependencies removed - handled by external service
# Pillow>=10.0.0         # Kept for general image processing if needed
numpy>=1.24.0            # Required by OpenCV
python-dotenv
mcp>=0.8.0
spacy>=3.7.0
librosa>=0.10.0
openai-whisper
psutil>=5.9.0
scipy>=1.10.0
python-dateutil>=2.8.2
python-frontmatter>=1.0.0  # YAML frontmatter for markdown documents
transformers>=4.30.0
torch>=2.0.0

pydantic[dotenv]>=1.10.0

# Semantic Search v2 Dependencies (Added 2025-08-26)
whoosh==2.7.4             # BM25 indexing for hybrid search
sentencepiece==0.1.99     # Fallback tokenizer for unknown models
boilerpy3==1.0.6         # OCR scan preprocessing to remove boilerplate
scikit-learn>=1.5.0      # RRF, metrics, and quality scoring
# Note: tiktoken, spacy, numpy, scipy already installed above
